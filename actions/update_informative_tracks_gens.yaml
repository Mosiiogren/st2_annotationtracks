---
name: update_informative_tracks_gens
pack: annotationtracks
description: Workflow to create and update informative annotationtracks in Gens
runner_type: orquesta
entry_point: workflows/update_informative_tracks_gens.yaml
enabled: true


parameters:
  hosts:
    description: The server the command is running at
    type: string
    default: "{{ config_context.hosts }}"
    immutable: True
  cwd:
    description: Path to the folder containing the docker-compose file
    type: string
    default: "{{ config_context.cwd }}"
    immutable: True
  

  
  
